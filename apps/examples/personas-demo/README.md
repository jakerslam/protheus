# Personas Demo

Internal toolkit demo for persona lens routing.

Run:

```bash
node apps/examples/personas-demo/run.js
```

This executes the public CLI contract:

```bash
target/debug/protheus-ops protheusctl toolkit personas --list
```
