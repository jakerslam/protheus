# Observability traces

This subdomain is part of the physical Observability domain. It contains runtime trace source maps and producer-to-Sentinel stream contracts.

Canonical contract:

- `sentinel_trace_source_map.json`
