# Installer Bootstrap Artifacts

These binaries are emergency bootstrap fallbacks used by `install.sh` when a release tag is missing required Linux assets.

Current artifacts:
- `protheus-ops-x86_64-unknown-linux-musl`
- `protheusd-x86_64-unknown-linux-musl`

Release artifacts remain the primary source; this directory is only for one-line installer continuity.
