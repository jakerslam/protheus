# 2026-03-10

- Confirmed workspace bootstrap was stale; removed `BOOTSTRAP.md` recoverably after verifying the workspace already had an established `SOUL.md`/policy state.
- Closed the client language-format gap in the live tree: `client` now has `0` tracked `.js`, `.sh`, `.py`, and `.ps1` files.
- Moved the IMAP/SMTP setup helper out of `client` to `scripts/skills/imap-smtp-email/setup.sh` so `client` stays TS/HTML only.
- Found that many renamed `.ts` wrappers were still invalid `ts_bootstrap` shells under Node 22 ESM execution.
- Rewrote the short cognition/runtime legacy-retired wrappers to ESM-compatible TS entrypoints backed by `legacy_retired_wrapper.ts`.
- Fixed `client/runtime/systems/compat/legacy_retired_lane.ts` argument parsing for `--lane-id=...`.
- Verified direct wrapper execution works for representative runtime/cognition wrappers and all policy audits + `./verify.sh` passed.
- Remaining client problem is wrapper volume, not language debt: live scope inventory shows `898` TS files with `679` in `client/runtime/systems` and `667` wrapper-class files.

## 2026-03-10 22:47 MT - client boundary objective checkpoint
- Replaced stale `git ls-files` client scope/debt measurement with live filesystem scans in `scripts/ci/client_scope_inventory.mjs` and `scripts/ci/client_legacy_debt_report.mjs`.
- Added and wired `client_surface_disposition.mjs` + `client_target_contract_audit.mjs` into `package.json` and `verify.sh`.
- Removed all live JS/SH/PY/PS1 hard violations from `client`; hard target-contract violations are now `0`.
- Re-homed non-client payloads from `client` into correct surfaces:
  - persona project trees -> `apps/personas/rohan-kapoor/projects/*`
  - skill packages with python scripts -> `adapters/skills/*`
  - runtime tmp/build/local debris -> `tmp/` and `state/local/`
- Converted clean allowlist entries into real client shims with authoritative code moved out:
  - `adapters/economy/smart_contract_bridge.ts`
  - `adapters/importers/*`
  - `apps/examples/singularity-seed-demo/orchestrator.ts`
- Kept `bird_x` and `ollama_search` in the client allowlist for now because their true collector logic still depends on cognition internals; reducing those further needs a proper cognition extraction, not a fake move.
- Current truthful target metrics:
  - total_ts_files=926
  - runtime_system_surface=687
  - cognition_surface=100
  - runtime_sdk_surface=59
  - wrapper_count=673
  - allowed_non_wrapper_count=15
- Policy gates passing: `ops:client-layer:boundary`, `ops:repo-surface:audit`, `ops:public-platform:contract`, `ops:client-target:audit`, `./verify.sh`.
