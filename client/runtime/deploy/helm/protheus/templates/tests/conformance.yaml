{{- if .Values.conformance.enabled }}
apiVersion: v1
kind: Pod
metadata:
  name: {{ include "protheus.fullname" . }}-conformance
  labels:
    app.kubernetes.io/name: {{ include "protheus.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
  annotations:
    "helm.sh/hook": test
spec:
  restartPolicy: Never
  containers:
    - name: conformance
      image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
      command:
        - /bin/sh
        - -c
        - |
          test -n "$NODE_ENV"
          test "$NODE_ENV" = "production"
          test -n "$PROTHEUS_SSO_ENABLED"
      envFrom:
        - configMapRef:
            name: {{ include "protheus.fullname" . }}-runtime
{{- if or .Values.secrets.create .Values.secrets.existingSecretName }}
        - secretRef:
            name: {{ include "protheus.secretName" . }}
            optional: {{ .Values.secrets.optional }}
{{- end }}
{{- end }}
