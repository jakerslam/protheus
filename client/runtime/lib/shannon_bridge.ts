#!/usr/bin/env node
'use strict';

// TypeScript compatibility shim only.
// Layer ownership: adapters/runtime::shannon-bridge (authoritative shared workflow bridge).

module.exports = require('../../../adapters/runtime/shannon_bridge.ts');
