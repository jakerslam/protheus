#!/usr/bin/env node
'use strict';

// TypeScript compatibility shim only.
// Layer ownership: adapters/runtime::infring-kernel-bridge (authoritative shared transport helper).

module.exports = require('../../../adapters/runtime/infring_kernel_bridge.ts');
