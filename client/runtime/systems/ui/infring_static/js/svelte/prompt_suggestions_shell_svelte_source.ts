const COMPONENT_TAG = 'infring-prompt-suggestions-shell';

const COMPONENT_SOURCE = String.raw`<svelte:options customElement="infring-prompt-suggestions-shell" />
<slot />
`;

module.exports = {
  COMPONENT_TAG,
  COMPONENT_SOURCE,
};
