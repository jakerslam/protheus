const COMPONENT_TAG = 'infring-chat-archived-banner-shell';

const COMPONENT_SOURCE = String.raw`<svelte:options customElement="infring-chat-archived-banner-shell" />
<slot />
`;

module.exports = {
  COMPONENT_TAG,
  COMPONENT_SOURCE,
};
