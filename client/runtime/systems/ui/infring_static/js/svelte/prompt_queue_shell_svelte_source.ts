const COMPONENT_TAG = 'infring-prompt-queue-shell';

const COMPONENT_SOURCE = String.raw`<svelte:options customElement="infring-prompt-queue-shell" />
<slot />
`;

module.exports = {
  COMPONENT_TAG,
  COMPONENT_SOURCE,
};
