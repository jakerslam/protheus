const COMPONENT_TAG = 'infring-workspace-panel-shell';

const COMPONENT_SOURCE = String.raw`<svelte:options customElement="infring-workspace-panel-shell" />
<slot />
`;

module.exports = {
  COMPONENT_TAG,
  COMPONENT_SOURCE,
};
