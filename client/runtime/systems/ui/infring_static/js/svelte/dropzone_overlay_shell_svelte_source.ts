const COMPONENT_TAG = 'infring-dropzone-overlay-shell';

const COMPONENT_SOURCE = String.raw`<svelte:options customElement="infring-dropzone-overlay-shell" />
<slot />
`;

module.exports = {
  COMPONENT_TAG,
  COMPONENT_SOURCE,
};
