const COMPONENT_TAG = 'infring-message-terminal-shell';

const COMPONENT_SOURCE = String.raw`<svelte:options customElement="infring-message-terminal-shell" />
<slot />
`;

module.exports = {
  COMPONENT_TAG,
  COMPONENT_SOURCE,
};
