const COMPONENT_TAG = 'infring-message-progress-shell';

const COMPONENT_SOURCE = String.raw`<svelte:options customElement="infring-message-progress-shell" />
<slot />
`;

module.exports = {
  COMPONENT_TAG,
  COMPONENT_SOURCE,
};
