const COMPONENT_TAG = 'infring-chat-header-shell';

const COMPONENT_SOURCE = String.raw`<svelte:options customElement="infring-chat-header-shell" />
<slot />
`;

module.exports = {
  COMPONENT_TAG,
  COMPONENT_SOURCE,
};
