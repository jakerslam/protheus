const COMPONENT_TAG = 'infring-message-artifact-shell';

const COMPONENT_SOURCE = String.raw`<svelte:options customElement="infring-message-artifact-shell" />
<slot />
`;

module.exports = {
  COMPONENT_TAG,
  COMPONENT_SOURCE,
};
