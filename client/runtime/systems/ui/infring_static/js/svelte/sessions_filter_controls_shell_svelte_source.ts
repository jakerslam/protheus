const COMPONENT_TAG = 'infring-sessions-filter-controls-shell';

const COMPONENT_SOURCE = String.raw`<svelte:options customElement="infring-sessions-filter-controls-shell" />
<slot />
`;

module.exports = {
  COMPONENT_TAG,
  COMPONENT_SOURCE,
};
