const COMPONENT_TAG = 'infring-composer-lane-shell';

const COMPONENT_SOURCE = String.raw`<svelte:options customElement="infring-composer-lane-shell" />
<slot />
`;

module.exports = {
  COMPONENT_TAG,
  COMPONENT_SOURCE,
};
