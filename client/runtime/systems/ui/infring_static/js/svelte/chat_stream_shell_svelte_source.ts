const COMPONENT_TAG = 'infring-chat-stream-shell';

const COMPONENT_SOURCE = String.raw`<svelte:options customElement="infring-chat-stream-shell" />
<slot />
`;

module.exports = {
  COMPONENT_TAG,
  COMPONENT_SOURCE,
};
