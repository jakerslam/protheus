const COMPONENT_TAG = 'infring-message-context-shell';

const COMPONENT_SOURCE = String.raw`<svelte:options customElement="infring-message-context-shell" />
<slot />
`;

module.exports = {
  COMPONENT_TAG,
  COMPONENT_SOURCE,
};
