const COMPONENT_TAG = 'infring-popup-window-shell';

const COMPONENT_SOURCE = String.raw`<svelte:options customElement="infring-popup-window-shell" />
<slot />
`;

module.exports = {
  COMPONENT_TAG,
  COMPONENT_SOURCE,
};
