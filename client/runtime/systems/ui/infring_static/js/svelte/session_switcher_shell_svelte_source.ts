const COMPONENT_TAG = 'infring-session-switcher-shell';

const COMPONENT_SOURCE = String.raw`<svelte:options customElement="infring-session-switcher-shell" />
<slot />
`;

module.exports = {
  COMPONENT_TAG,
  COMPONENT_SOURCE,
};
