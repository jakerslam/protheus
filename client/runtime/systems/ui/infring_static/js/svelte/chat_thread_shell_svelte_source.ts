const COMPONENT_TAG = 'infring-chat-thread-shell';

const COMPONENT_SOURCE = String.raw`<svelte:options customElement="infring-chat-thread-shell" />
<slot />
`;

module.exports = {
  COMPONENT_TAG,
  COMPONENT_SOURCE,
};
