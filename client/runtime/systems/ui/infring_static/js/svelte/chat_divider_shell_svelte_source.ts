const COMPONENT_TAG = 'infring-chat-divider-shell';

const COMPONENT_SOURCE = String.raw`<svelte:options customElement="infring-chat-divider-shell" />
<slot />
`;

module.exports = {
  COMPONENT_TAG,
  COMPONENT_SOURCE,
};
