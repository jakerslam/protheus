#!/usr/bin/env tsx

// TypeScript compatibility shim only.
// Layer ownership: adapters/runtime::agent-ws-bridge (dashboard websocket surface adapter).

module.exports = require('../../../../adapters/runtime/agent_ws_bridge.ts');
